Database setup

CREATE TABLE events (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    date DATE NOT NULL,
    status ENUM('open','closed') DEFAULT 'open',
    poster VARCHAR(255)
);